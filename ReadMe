# ✨ 句子相似度评估平台（ChatChat vs 通义千问）

该项目用于对比两个大语言模型（本地部署的 ChatChat 和通义千问 Qwen）的文本输出内容，通过语义相似度和语言模型评估两种方式对生成内容进行分析。

---

## 🚀 功能说明

- ✅ 连接本地部署的 ChatChat 服务（deepseek-chat 模型）
- ✅ 调用阿里云 DashScope 接口获取通义千问的文本生成
- ✅ 基于 SentenceTransformer 向量模型对输出文本进行语义相似度评估
- ✅ 使用 LLM 模型进行语言理解层面的句子对比
- ✅ 支持用户交互输入自定义情境提示词
- ✅ 支持连续输入（循环交互）

---

## 📦 安装依赖

确保你使用的是 Python 3.8+ 的环境，并使用 `pip` 安装依赖：
pip install -r requirements.txt

🛠 使用指南

1. 启动 ChatChat 本地服务（如你部署在 Linux 虚拟机）
确保通过 cpolar 成功开启公网隧道，并更新代码中地址，例如：
API_ENDPOINT = "http://xxxxx.rxx.cpolar.top/chat/chat/completions"

2. 启动主程序
test_chatchat_similarity_plus.py
主程序会首先生成一个默认情景句子，然后提示用户可以输入自定义提示词进行相似度评估。

🔍 模型与技术
本地模型	ChatChat (deepseek-chat)
云模型	通义千问（qwen-plus）
相似度评估	SentenceTransformer: paraphrase-multilingual-MiniLM-L12-v2
语言评估（可选）	GPT / ChatGPT 等 API
网络代理	cpolar

📝 示例输出

请输入一个情景提示语（输入 exit 可退出）：请描述一场春日湖边的野餐。

从 ChatChat 获取的句子: 在温暖的春日下午，一家人坐在湖边铺着格子布的草地上，享受着阳光与美食。
从千问获取的句子: 春天的午后，几位朋友围坐在湖边野餐，微风拂面，欢笑声此起彼伏。

🔍 向量模型评估结果：
cosine_similarity: 0.8941
euclidean_distance: 1.2294

🔍 大模型评估结果：
语义相似度评分: 4.5/5
结构匹配度: 高
...

🔐 注意事项
使用 DashScope 千问 API 时需要提供有效的 API Key
ChatChat 的公网地址需保持有效（每次重启前需确认）
若 sentence-transformers 加载模型缓慢，请提前预加载以提升体验

📂 文件结构建议
project/
│
├── main.py                     # 向量模型评估demo
├──AiEvo.py                     # 大模型评估demo
├── test_chatchat_similarity_plus.py      # 相似度评估模块
├── requirements.txt            # Python 依赖项
└── README.md                   # 项目说明文件

